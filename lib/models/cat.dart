class Cat {
  final String name;
  final String imagePath;
  final String description;
  final String color; // 고양이의 색상 정보

  Cat({
    required this.name,
    required this.imagePath,
    required this.description,
    required this.color, // 색상 매개변수 추가
  });
}
