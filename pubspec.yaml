name: chat_cat_game
description: "A Flutter project integrating a game and a chatbot."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.22.0
  http: ^1.2.2 # 최신 버전 확인 후 조정 가능
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/background.png
    - assets/images/grayCat.png
    - assets/images/grayCat_open_mouth.png
